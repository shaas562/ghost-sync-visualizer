plugins {
    id 'base'
}

allprojects {
    group = project.maven_group
    version = project.mod_version
}

tasks.register('verifyMilestone1') {
    group = 'verification'
    description = 'Runs the version-independent core tests and builds the Fabric client module.'
    dependsOn ':core:test', ':fabric:build'
}
